# !/bin/bash

time_stamp=$(date +%m-%d-%H-%M-%S)
mkdir "$HOME/uav/save/${time_stamp}"
for f in $HOME/uav/save/*.jpg; do
mv "$f" "$HOME/uav/save/${time_stamp}"
done

for f in $HOME/uav/save/*.log; do 
mv "$f" "$HOME/uav/save/${time_stamp}"
done
